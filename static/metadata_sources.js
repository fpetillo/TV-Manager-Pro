(function(){const form=document.getElementById('tvdbConfig'),key=document.getElementById('tvdbKey'),pin=document.getElementById('tvdbPin'),status=document.getElementById('tvdbStatus');
fetch('/api/metadata-sources/tvdb').then(async r=>{if(!r.ok)throw new Error('Could not load TVDB settings');const d=await r.json();key.value=d.api_key;pin.value=d.pin;status.textContent=d.configured?'TVDB credentials saved. Choose TVDB in Add a Show.':'TVDB is not configured.';}).catch(e=>status.textContent=e.message);
form.onsubmit=async e=>{e.preventDefault();const button=form.querySelector('button');button.disabled=true;try{const r=await fetch('/api/metadata-sources/tvdb',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({api_key:key.value,pin:pin.value})});if(!r.ok)throw new Error('Save failed');status.textContent='TVDB settings saved. Credentials are checked when you search or refresh.';}catch(ex){status.textContent=ex.message;}finally{button.disabled=false;}};
})();
