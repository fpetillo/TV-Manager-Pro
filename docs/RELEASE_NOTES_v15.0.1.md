# TV Manager 15.0.1

Documentation/setup hotfix focused on repeatable installation on another Windows PC.

- Adds `docs/WINDOWS_FRESH_INSTALL.md` as the canonical install/migration guide.
- Documents PowerShell execution policy commands.
- Documents dependency installation and the Waitress verification/repair commands.
- Documents `.env` and `tvmanager.db` preservation.
- Documents `run.ps1`, `run-prod.ps1`, `install-production-startup.ps1`, and `status.ps1`.
- Documents UNC/network-share behavior for Scheduled Tasks.
- Documents common startup and port-conflict troubleshooting.
- Improves `setup.ps1` preflight and dependency verification.
