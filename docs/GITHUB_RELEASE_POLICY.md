# GitHub Release Policy

Every release must update source, VERSION, CHANGELOG, relevant docs, tests, CI, and release notes together. Runtime databases, `.env`, logs, backups, diagnostics and managed trash must never be committed.
